pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'No Build step needed for this pipeline'
            }
        }

        stage('Test') {
            steps {
                echo 'No test steps needed for this pipeline'
            }
        }
    }
}

